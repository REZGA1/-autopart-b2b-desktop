{
  "name": "autopart-b2b-desktop",
  "version": "1.0.0",
  "description": "AutoPart B2B Desktop Application",
  "author": "AutoPart B2B Team",
  "main": "electron.js",
  "scripts": {
    "dev": "electron .",
    "prebuild": "cd .. && npx vite build --config vite.config.mjs --mode production",
    "postbuild": "node scripts/copy-renderer.js",
    "build": "npm run prebuild && npm run postbuild",
    "dist:win": "npm run build && npx electron-packager . AutoPartB2B --overwrite --platform=win32 --arch=x64 --icon=icon.png --prune=true --out=dist-packaged --ignore=\"^/(dist-packaged|scripts|electron-builder\\\\.json)\"",
    "dist:installer": "npm run build && npx electron-builder build --win"
  },
  "devDependencies": {
    "electron": "^35.0.0",
    "electron-builder": "^26.0.0",
    "electron-packager": "^17.1.2"
  }
}
